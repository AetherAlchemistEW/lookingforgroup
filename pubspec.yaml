---
name: lookingforgroup
description: Find people to do stuff with, chat with your friends while you're out
  doing things.
publish_to: none
version: 1.0.0+1
environment:
  sdk: '>=2.17.0-69.2.beta <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.0.15
  firebase_auth: ^4.1.2
  firebase_core: ^2.2.0
  cloud_firestore: ^4.0.5
  firebase_analytics: ^10.0.5
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true
  assets:
  - assets/LoginScreen.png
  - assets/RegisterScreen.png
